<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Página Principal</title>
</head>

<body>
  <h1>Página de Inicio</h1>
  <!--cuando usamos <%= %> la plantilla entiende que se envian datos
  desde node
  <h2>El usuario conectado es : <%= %> </h2>-->
  <% if (login) { %>
    <h1>El usuario conectado es : <strong><%= name %></strong></h1>
    <a href="logout" class="a-logout">Cerrar Sesión</a>
  <% } else { %>
    <h1><strong><%= name %></strong></h1>
    <a href="login" class="a-login">Iniciar Sesión</a>
  <% } %>
</body>
</html>